# Project Brain - Master Memory

**Read this file FIRST at the start of every session.**

## Overview
Trialshopy is an e-commerce platform consisting of 5 main components:
1. **Customer Frontend**: Next.js application for end-users.
2. **Customer Backend**: Node.js/Express application with MongoDB and Redis.
3. **Seller Admin Dashboard**: Vite/React frontend for sellers to manage their store.
4. **Seller Admin Backend**: Node.js/Express backend for seller operations.
5. **Super Admin Dashboard**: Vite/React frontend for platform administrators.

## Architecture Highlights
- **Frontend Stack**: React 18, Next.js (Customer) / Vite (Admins), Tailwind CSS, Firebase (Auth/Notifications), Socket.io (Real-time).
- **Backend Stack**: Node.js, Express, MongoDB (Mongoose), Redis (Caching), Cloudinary (Media), Razorpay (Payments), Socket.io (WebSockets).

## Key Patterns
- **Auth**: JWT based authentication (Backend), Firebase Auth integration in some frontends.
- **API**: RESTful APIs with Axios in frontends.
- **State/UI**: Tailwind CSS for styling, Ant Design / MUI used in admin panels.

## Known Issues & Roadmaps
- (To be updated as issues are encountered and resolved)

## Recent Features
- **Phone-OTP-first Login + Auto-Signup (2026-07-02)**: (1) **Core bug fix** in `Trialshopy-backend-master/src/api/controllers/loginAuth.controller.ts` `verifyOTP`: previously, an unregistered phone returned "User not found" but did NOT `return`, then called `generateToken(...null)` → crash; new numbers could neither log in nor sign up. Now after a valid OTP: find user by `phone_number`; **if not found, auto-create a minimal account** (`name: "User <last4>"`, synthetic unique `email: <digits>@phone.trialshopy.in` to satisfy the required/unique email field, password uses model default `"00000000"`) and log them in. Response adds `isNewUser` flag. Zero-friction signup like Flipkart/Meesho. (2) **Frontend** made phone/OTP the PRIMARY method: `LoginPage.jsx` default `loginMethod` now `"otp"` (was `"pwd"`); email+password demoted to a secondary outline button. Restyled all three auth files to site theme (`#EB8105` / `from-[#FAAC06] to-[#EB8105]` gradient, rounded cards). `LoginwithOTP.jsx`: fixed `+91` prefix affix + 10-digit validation + loading state + "account created automatically" helper. `OTPpage.jsx`: themed, **working Resend OTP** (re-calls `/send-otp`, 120s cooldown; removed the old effect that kicked user back to number entry after timeout), added "Change mobile number" back button. NOTE: backend runs from compiled `dist/` (`node dist/server.js` in start.bat), so the controller fix requires **rebuilding the backend** (`npm run build`) and restarting for it to take effect. OTP is logged to backend console by `authService.sendOTP` (Twilio optional).
- **Product Page Perf + UI + Bestsellers (2026-07-02)**: (1) **Perf**: `src/components/productDetails/ProductDetails.jsx` was fetching similar products with NO limit (pulled the whole category, populated seller/store/categories) — the main slowness. Now all recommendation calls use `POST /api/v1/products?limit=12`; product/reviews/similar/bestsellers fetch in parallel (similar was previously nested inside the product fetch); added a `ProductSkeleton` mirroring the layout instead of the blank full-page `<Loading/>`; hero `<Image>` marked `priority` + `sizes`. Added `isMounted` cleanup guard. (2) **Bestsellers section**: new `src/components/productDetails/Bestsellers.jsx` — standard e-commerce recommendation row. No sold/order-count exists on the product model, so "popularity" = `rating.count`; fetched via `sortBy: "-rating.count"` (Product `getAll2` passes sortBy to Mongoose `.sort()`). Reuses `SimilarProductCard`, shows #1–#3 rank badges, current product filtered out. Rendered above Similar Products. (3) **UI polish** in `ProductDetailsComponent.jsx`: added breadcrumb (Home/Products/name), "Inclusive of all taxes" note, a 4-item trust-badge strip (Free Delivery / 7-Day Return / Secure Payment / 100% Genuine). **Fixed discount bug**: `discountPercentage` used `(discountedPrice - MRP)/MRP` → negative; now `(MRP - discountedPrice)/MRP` with divide-by-zero guard (`currentMRP`). All cart/size/buy/live-demo/try-on logic unchanged. Verified with `next build` (compiled successfully, 170 pages).
- **Return-after-delivery + Payment/Confirmation UI (2026-07-02)**: (1) **Return gating**: `src/components/account/OrderDetails.jsx` previously showed the Return button whenever `item.status !== "return"` (i.e. immediately after ordering). Now Return only appears once the item is delivered (`statusMap["delivered"]`) AND the return window is open (`data.exchangeReturnWindowClosedOn`). States: not-delivered → "Return available after delivery"; delivered+open → Return button (with `window.confirm` + `router.refresh()`); already requested → "Return Requested" badge; window passed → "Return window closed". Status flow is `placed→processed→shipped→delivered`. (2) **UI polish**: redesigned `src/components/payments/paymentpage.jsx` into a 2-column layout (address + selectable payment-method cards on the left, sticky price-summary card on the right) and `src/components/payments/confirmation.jsx` into a clean success card (green check, order summary strip, item grid, Continue Shopping / Track Order buttons). All business logic/handlers (`handleOpenRazorpay`, `handleReturn`, price calc) unchanged.
- **Virtual Try-On UI Remake (2026-07-02)**: Rebuilt `src/app/(clayout)/virtualTryOn/page.jsx` to match the site's design language (solid `#EB8105` / `from-[#FAAC06] to-[#EB8105]` gradient, `rounded-md`, `shadow-sm`, `font-semibold` — flat, not the old `rounded-3xl`/`scale-105`/gradient-bg styling). **Removed Video Try-On entirely** from the UI: dropped the Photo/Video toggle, `mode` and `isVideoData` state, and all video capture/upload/result rendering. Simplified to a clean 2-step layout (Step 1 Your Photo via Camera or Upload, Step 2 Garment) + a single "Try It On" button + result with Download. All backend logic (`/upload-person`, `/gradio` endpoints, retry + queue-status handling) is unchanged; backend video support still exists but is no longer reachable from this page.
- **Gradio Queue API Virtual Try-On (CatVTON)**: No GPU locally (CPU-only). Created `tryon_server.py` - a Flask bridge that wraps `gradio_client` to call the CatVTON HF Space (`zhengchong/CatVTON`). Exposes `/trial/api/join_queue/` and `/trial/api/queue_data/` endpoints at `http://127.0.0.1:8000`. Start with `start_tryon.bat`. Backend polls up to 6 min (72 x 5s). Frontend timeout is 7 min. `VIRTUAL_TRYON_API=http://127.0.0.1:8000` in backend `.env`. All files: `tryon_server.py`, `tryon_requirements.txt`, `start_tryon.bat`, `src/services/gradioTryOn.service.ts`, `src/api/controllers/tryon.controller.ts`, `POST /api/v1/tryon/gradio` route.
- **Gradio Try-On ECONNRESET Fix**: Cloudinary upload of person image was failing with ECONNRESET due to large base64 payload. Fixed by: (1) `gradioTryOn.service.ts` now uses `upload_stream` (Buffer-based) with 3 retry attempts instead of `uploader.upload(base64string)`. (2) New `POST /api/v1/tryon/upload-person` endpoint dedicated to uploading the person image with retry logic. (3) Frontend `virtualTryOn/page.jsx` now performs a 2-step process: upload person image first (up to 3 retries), then call gradio with the resulting Cloudinary URL instead of raw base64.
- **Video Try-On**: Expanded the Virtual Try-On page to support videos. Added a toggle between Photo and Video try-on. Backend `tryon.controller.ts` modified to handle video payload correctly (Cloudinary `resource_type: auto` and FitRoom `person_video` key).
- **FitRoom Virtual Try-On**: Replaced Hugging Face integration with FitRoom API. Created `/api/v1/tryon/generate` endpoint in backend that scales garment images to 1024px, uploads person images, and polls FitRoom. Frontend `virtualTryOn` page updated to use this endpoint.
- **Virtual Try-On Image**: Added an optional upload for Virtual Try-On images in Seller Admin Dashboard. Modifies `product.model.ts` (adding `tryOnImage` and `tryOnEnabled`) and shows a "Try It On" button on the customer frontend's product details page.
- **Category Page Performance (2026-07-02)**: Multi-layer performance fix for slow subcategory pages:
  1. **ISR + generateStaticParams** (`src/app/(clayout)/subcategory/[id]/page.js`): Pages are now pre-built at build time using `generateStaticParams()` fetching all category IDs from `/api/v1/homeCategories`. ISR revalidates every 3600s. The helper recursively collects IDs from all nesting levels of the category tree.
  2. **Skeleton Loading**: Already in place via `loading.js` + `SkeletonLoader.jsx`. No changes needed.
  3. **Link Prefetching**: All navbar/sidebar category links already use Next.js `<Link>`. `SubCatCol.js` and `CategoryComponent.js` confirmed correct.
  4. **MongoDB Compound Index** (`product.model.ts`): Added `{ category: 1, status: 1 }` and `{ categories: 1, status: 1 }` indexes on the Product collection.
  5. **Redis Caching** (`product.service.ts`): `getAll2` now caches results under key `products:category:{sortedIds}:page:{n}` with 30-minute TTL. Cache is invalidated (SCAN + DEL) on `create`, `update`, and `delete` for the affected categories. All Redis errors are non-fatal and logged only.

## Update Rule
After finishing any task (bug fix, new feature), **update this and the relevant brain files** with what changed.

## Security Notes
- **GitHub Push Protection Fix (2026-06-30)**: Removed all hardcoded secret fallbacks from source files. Affected: `loginAuth.service.ts`, `testOtp.controller.ts`, `database.config.ts`, `app.ts`, `download.contoller.ts`, `upload.middleware.ts` (customer backend) and `sellerAuth.controller.ts` (seller backend). All now use strict `process.env.VAR!` with no fallback. `.env.example` files added to both backends. Git history rewritten via `git commit --amend` (old hash: `4e2af26` → new: `3eba13e`). Force push required: `git push --force-with-lease origin main`.
- **Frontend URL Refactor (2026-06-30)**: Removed all hardcoded `localhost` and `onrender.com` URLs across `Trialshopy_Frontend_25-main`, `SELLER-ADMIN-DASHBOARD-main`, and `Super-Admin-main`. All frontends now strictly use environment variables (`process.env.NEXT_PUBLIC_*` or `import.meta.env.*`) for backend and seller endpoints, making them production-ready for deployment.
